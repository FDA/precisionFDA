export const spaceMembersListIsFetchingSelector = (state) => state.spaces.members.isFetching
export const spaceMembersListSelector = (state) => state.spaces.members.entries
export const spaceMembersAddModalSelector = (state) => state.spaces.members.addMembersModal
export const spaceMembersAddErrorsSelector = (state) => state.spaces.members.errors
export const spaceMembersCheckRoleSelector = (state) => state.spaces.members.roleChangeChecks
export const spaceMemberRoleChangeModalSelector = (state) => state.spaces.members.roleChangeModal
