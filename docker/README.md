# Important note

Before editing **any** compose files, read the [Docker compose guide](../docs/DOCKER_COMPOSE_GUIDE.md). Local Docker uses `docker/dev.docker-compose.yml`, which is architecture-agnostic — it works on Apple Silicon and x86 hosts without any overrides.
