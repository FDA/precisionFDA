{
  "name": "precision-fda",
  "version": "1.0.0",
  "description": "Precision FDA",
  "main": "index.js",
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "compose:up": "docker compose --env-file docker/.env -p precision-fda -f docker/arm64v8.dev.docker-compose.yml up --build"
  },
  "repository": {},
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "2.4.15"
  }
}
