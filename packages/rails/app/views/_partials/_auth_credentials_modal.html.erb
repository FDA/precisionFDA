<div id="auth_credentials_modal" class="modal fade tasks-actions-modal modal-with-loader" data-bind="css: {loading: authCredentialsModal.isLoading}">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <h4 class="modal-title">Enter credentials</h4>
      </div>
      <div class="loader-container">
        <div class="loader">
          <i class="fa fa-spinner fa-spin"></i>
        </div>
      </div>
      <div class="modal-body">
        <p>
          <div class="input-row pfda-padded-b">
            <div class="pull-right">
              <a href="javascript:void(0)"
                 data-html="true"
                 data-trigger="focus"
                 data-toggle="popover"
                 data-title="Two-Factor Authentication"
                 data-content="This account is set up to require two-factor authentication at login. This is an optional feature that requires a verification code in addition to a password. Typically, the code is generated by the Google Authenticator app on a mobile device.<br><br>Please do not reuse the same code. Instead, wait briefly to enter a newly generated code.<br><br>If you have trouble retrieving your verification code, or get locked out of your account accidentally, contact <a href=&quot;mailto:support@dnanexus.com&quot;>Support</a>."
              >How do I do this?</a>
            </div>
          </div>
          <div class="input-row pfda-padded-b">
            <input
              id="auth_credentials_modal_cridentials_input"
              type="password"
              class="form-control"
              placeholder="Password"
              data-bind="event: {input: authCredentialsModal.onPasswordInputValue}"
            />
          </div>
          <div class="input-row">
            <input
              id="auth_credentials_modal_opts_input"
              type="text"
              class="form-control"
              placeholder="One-time password"
              data-bind="event: {input: authCredentialsModal.onOtpInputValue}"
            />
          </div>
        </p>
      </div>
      <div class="modal-footer">
        <button class="btn btn-default" data-dismiss="modal">Cancel</button>
        <button class="btn btn-primary" data-bind="enable: authCredentialsModal.enableButton, click: authCredentialsModal.modalAction">
          Update
        </button>
      </div>
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->
