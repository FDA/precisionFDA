<h2>
  Your Expert Q&A Dashboard
</h2>
<p>
  <br/>
  <%= image_tag(@expert.image, class: 'rounded', height: '350px', alt: "Expert's profile picture") %>
</p>

<p>
  <% if @expert.open? %>
    <span class="badge badge-success open-button"><span class="fa fa-check"></span> Open</span>
  <% else %>
    <span class="badge badge-default closed-button"><span class="fa fa-lock"></span> Closed</span>
  <% end %>
  <% if @expert.editable_by?(@context) %>
    <% if @expert.is_public? %>
      <span class="badge badge-primary"><span class="fa fa-eye"></span> Public</span>
    <% else %>
      <span class="badge badge-warning private-button"><span class="fa fa-eye-slash"></span> Private</span>
    <% end %>
  <% end %>
</p>
