<div role="tabpanel" class="tab-pane active">
  <% if @challenge.can_show_results?(@context) %>

    <div class="content_tools">
      <%= raw(challenge.regions["results"]) %>
    </div>

    <br>

    <div class="content_tools">
      <%= raw(challenge.regions["results-details"]) %>
    </div>

  <% end %>
</div>
