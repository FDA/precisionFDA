<% content_for :title do %>Create a new Participant<% end %>

<%= render partial: "admin/_partials/workbench_toolbar" %>

<div class="page-header">
  <div class="page-title">
    <span class="fa fa-inbox"></span>
    Create a new Participant
  </div>
</div>

<%= render 'form' %>
