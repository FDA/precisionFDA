<% if @context.logged_in? %>
<div class="pfda-comment">
  <div class="media">
    <div class="media-body">
      <div class="pfda-comment-body">
        <%- comment = comment || Comment.new %>
        <%= render "comments/form", comment: comment, url: url %>
      </div>
    </div>
  </div>
</div>
<% end %>
