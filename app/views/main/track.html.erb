<div class="track-intro">
  <%= alert_help("Learn more about tracking", "#{about_path}/how#track") %>
</div>
<div class="track-canvas-wrapper">
  <svg class="track-canvas"><g/></svg>
</div>

<script src="https://cpettitt.github.io/project/dagre-d3/latest/dagre-d3.js"></script>
<script>
var g = new dagreD3.graphlib.Graph().setGraph({});

<%= raw graph_nodes(@graph) %>
<%= raw graph_edges(@graph) %>

var svg = d3.select("svg.track-canvas"),
    inner = svg.select("g");

var render = new dagreD3.render();
render(inner, g);

svg.attr("height", g.graph().height + 2);
svg.attr("width", g.graph().width + 2);
</script>
