<% content_for :title do %><%= @app.title %><% end %>

<div class="page-header">
  <div class="page-title">
    <span class="fa fa-edit"></span>
      Editing <small><a href="<%= app_path(@app) %>" data-bind="text: title"><%= @app.title %></a></small>
  </div>
</div>

<%= render "apps/editor", app: @app %>
