<% content_for :title do %>Meta Appathons<% end %>

<% if @context.user.can_administer_site? %>
<div class="pfda-toolbar">
  <div class="btn-group">
    <%= link_to new_meta_appathon_path, class: 'btn btn-primary' do %>
      <span class="fa fa-plus fa-fw"></span> Create a meta appathon
    <% end %>
  </div>
</div>
<% end %>

<%= render "meta_appathons/cards", meta_appathons: @meta_appathons %>
