<% if @context.can_administer_site? || @context.challenge_admin?%>
  <div data-editable data-name="pre-registration" class="container container-padded" id="pre-registration">
    <%= raw(challenge.regions["pre-registration"]) %>
  </div>
<% end %>
