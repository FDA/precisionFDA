<% if @context.logged_in? && @context.user.can_administer_site? %>
  <div data-editable data-name="intro" class="container container-padded" id="intro">
    <%= raw(challenge.regions["intro"]) %>
  </div>
<% end %>
