<li class="<%= classes %>">
  <div class="variant-input-cell">
    <div class="file-label"><span class="fa fa-fw fa-file-o"></span> <span><%= title %></span></div>
    <% if !required  %>
      <div class="file-optional">Optional</div>
    <% end %>
  </div>
  <div class="variant-input-cell">
    <% if file %>
      <span class='access-icon fa fa-fw <%= file.public? ? "fa-globe" : "fa-lock" %>'></span>
      <%= link_to(file.name, file_path(file.dxid))%>
    <% else %>
      <span class="text-muted">Not set</span>
    <% end %>
  </div>
</li>
